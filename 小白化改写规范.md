# 数学教程「小白化」改写规范

> 版本：v1.0（2026-08-20）
> 适用范围：`src/` 下第 01–151 级各章节文件
> 用途：作为大模型（或人工）批量改写的统一执行标准，配套附录 A 的提示词模板使用

---

## 1. 改写目标与读者假设

### 1.1 读者画像（"小白"的定义）

- **第 01–08 级**：零基础读者，不假设任何数学背景，连符号记法也需要解释
- **第 09 级起**：假设读者已读毕本级之前的直接前置级别，**对本章内容一无所知**
- **共性假设**：读者不具备"自行补课"的能力——凡本级用到的非平凡概念，要么已在前文出现过，要么必须当场解释

### 1.2 核心策略：叠加式改写，不做重写

原文的严谨定义、定理表述、推导与习题**一字不动**。所有"小白化"内容以**插入新板块**的方式叠加在原文之上：

1. 零风险——不会因为改写引入数学错误
2. 可验收——插入内容可整体摘出检查
3. 分层阅读——想打基础的读插入板块，想严谨的读原文

---

## 2. 五条铁律（优先级高于一切）

1. **只插不改**：禁止改写、删减、润色任何原有内容（定义、定理、证明、习题、总结、图片、链接）
2. **数学保真**：插入的解释、类比、符号说明不得与原文冲突，不得引入原文没有且未加说明的记号
3. **格式不破**：公式 `$$` 成对、行内 `$` 单层成对、`$$` 块行首不缩进 4 空格（详见 §5）
4. **编号不动**：不得重排原有章节编号；新板块一律使用**无编号标题**
5. **习题禁区**：`## 习题` 与 `## 习题答案与解析` 两节（含其下所有小节）绝对不改

---

## 3. 四个插入动作（改写范围）

每章只执行以下四个动作，除此之外不做任何其他改动。

### 3.1 动作一：章首插入「开始之前」

**位置**：紧跟「学习目标」节末尾之后，作为无编号章节 `## 开始之前`。若该章使用 `---` 分隔章节，新章节前后也加 `---`。

**内容三要素**（缺一不可）：

1. **本章是什么**：2–3 句话，完全不含公式
2. **为什么要学**：指出本章解决前面级别遗留的哪个问题 / 为后面第几级铺路（能精确到"第 X 级"必须精确）
3. **开始前请确认你还记得**：3–5 条前置概念，每条格式为「概念名（见第 X 级）：一句话唤醒记忆」

**格式示例**：

```markdown
## 开始之前

**本章在讲什么**：……

**为什么要学它**：……

**开始前请确认你还记得**：
- 自然数与整数的区别（第 1 级）：整数比自然数多了负数和 0……
```

### 3.2 动作二：核心公式前插入「符号速查表」

**触发条件**（满足任一条即插入）：

- 含 3 个及以上陌生符号的公式
- 本章第一次出现的"定义型"公式
- 使用了非标准记号（如 $\mathbb{Z}$ 来自德语 Zahlen 这类冷知识，可并入表中）

**位置**：紧贴公式上方（公式为 `$$` 块时，插在块的上一行）。

**格式**（四列表格，列名固定）：

```markdown
| 符号 | 名称 | 含义 | 直观解释 |
|:---:|:---:|:---|:---|
```

**规则**：

- 一行一个符号；"直观解释"不超过 15 字，用生活语言
- 同一符号全章只做一次速查，再次出现时不重复
- 表格中的公式仍用行内 `$...$`

**示例**（针对第 1 级有理数定义公式）：

```markdown
| 符号 | 名称 | 含义 | 直观解释 |
|:---:|:---:|:---|:---|
| $\mathbb{Q}$ | 有理数集 | 全体能写成两整数之比的数 | "能写成分数的数" |
| $p$ | 分子 | 任意整数 | 分子随便取 |
| $q$ | 分母 | 任意非零整数 | 分母不能是 0 |
```

### 3.3 动作三：推导过程插入「逐步批注」

**触发条件**：步骤 ≥ 3 的证明 / 推导 / 计算过程。每章最多批注 3–5 处**最关键**的推导，简单的一两步推导不批注。

**规则**（按原推导形态二选一）：

- **形态 A（推导由多个 `$$` 块构成，块间有文字）**：在块与块之间插入批注列表项，格式 `- 【第 k 步】依据：……；目的：……`。原有 `$$` 块内容一个字符都不动
- **形态 B（推导是单个长公式块）**：在块的**之后**（不是里面）追加批注列表，逐个等号 / 变形解读，格式 `- 第 1 个等号：……，依据……`

**要求**：每条批注必须同时回答——

- **依据**：用了什么定理 / 性质 / 约定
- **目的**：这一步是为了逼近什么

### 3.4 动作四：章末插入「常见误区」

**位置**：全章最末（「总结」节之后），无编号章节 `## 常见误区`。若该章使用 `---` 分隔章节，新章节前也加 `---`。

**内容**：3–5 条，从本章概念中**真实高频**的误解里选。不确定时优先从这四类里挑：

1. 符号混淆（把 $A$ 的记号当成 $B$ 的）
2. 方向搞反（充分必要颠倒、包含关系颠倒）
3. 条件遗漏（忽略定理前提）
4. 类比过度（把直观类比当成了定理本身）

**每条固定三行格式**：

```markdown
### 误区 N：{一句话陈述错误理解}
**为什么错**：……
**正确理解**：{一句话}
```

**示例**：

```markdown
### 误区 1：「有理数就是有限小数」
**为什么错**：$\frac{1}{3}=0.333\dots$ 是无限循环小数，但它是有理数。
**正确理解**：有限小数与无限循环小数统称有理数。
```

---

## 4. 「直观理解」板块的统一约定

部分章节已存在 `> **直观理解（现实类比）**：...` 引用块，约定如下：

1. **已有则保留原样**，不修改、不合并、不删除
2. **新写类比一律沿用同一格式**：`> **直观理解（现实类比）**：`
3. **类比质量三条标准**：
   - **结构对应**：类比中的每个要素都能指回数学对象（写明"X 对应 Y"）
   - **失效边界**：至少一句话说明"类比在哪里不成立"
   - **不制造新误解**：类比本身不能蕴含错误的数学结论（如把极限说成"永远达不到"）
4. **数量控制**：每章新增类比 ≤ 3 个，只给全章最抽象的 2–3 个概念配类比

---

## 5. 格式硬约束（机械可检）

| 约束 | 检查方法 |
|:---|:---|
| `$$` 全文成对（总数为偶数） | 统计 `$$` 出现次数 |
| `$$` 块行首缩进 ≤ 3 空格 | 匹配 `^    \$\$` 应为 0 处 |
| 行内公式 `$...$` 成对且内部无孤立 `$` | 逐行检查 `$` 计数为偶数 |
| 习题两节内容逐字节与改写前一致 | diff 对比 |
| 图片引用 `![...](images/...)` 不增不减 | diff 对比 |
| 原有章节编号未变 | diff 所有 `^## ` 行 |

---

## 6. 禁止事项清单

- 禁止改写、删减、移动原有任何文字（哪怕发现疑似笔误——记录到汇报里，不要动手改）
- 禁止改动原文公式（包括"顺手修正"疑似错误）
- 禁止新增图片文件或图片引用（插图由人工另行安排）
- 禁止重排章节编号、修改 `SUMMARY.md`
- 禁止在「习题」「习题答案与解析」两节内插入任何内容
- 禁止引入原文未定义且未当场解释的新记号
- 插入内容禁止使用 emoji

---

## 7. 验收清单（每章改完后逐项自检）

1. 四个动作是否齐全：开始之前 / 符号速查表 ≥ 1 处 / 逐步批注 ≥ 1 处 / 常见误区？
2. §5 六项格式约束是否全部通过？
3. 抽查 2 个符号表条目、1 条推导批注：数学表述是否与原文一致？
4. 新增类比是否满足 §4 三条质量标准？
5. 「开始之前」的前置自检是否精确到级别号？

---

## 8. 批量执行流程

1. **试点**：先按本规范改写 1 章（建议第 01 级），人工验收
2. **定稿**：根据试点反馈修订本规范，形成 v1.1
3. **分批**：按目录（01–09 共 9 个目录）分批派发并行子代理，每批 1 个目录
4. **抽验**：每批完成后按 §7 抽验 20%，不合格整批返工
5. **收尾**：对 §5 六项机械检查写脚本全量跑一遍

---

## 附录 A：单章改写提示词模板（子代理直接使用）

```text
你要对数学教程的一个章节文件执行"小白化"改写。严格遵循以下规范，
规范优先级高于你的默认习惯。

【文件】{文件绝对路径}

【读者假设】假设读者已学完本级别之前的直接前置级别（第 1 级则假设零基础），
对本章内容一无所知，且不具备自行补课的能力。

【总原则】叠加式改写：原文一字不动，只允许插入以下四个板块的内容。
发现原文疑似笔误或错误时，只记录、不修改。

【动作 1】在「学习目标」节末尾之后，插入无编号章节「## 开始之前」，包含：
(a) 本章是什么：2-3 句话，完全不用公式；
(b) 为什么要学：指出它解决前面第几级遗留的问题 / 为后面第几级铺路；
(c) 开始前请确认你还记得：3-5 条前置概念，每条格式
    「概念名（见第 X 级）：一句话唤醒记忆」。
若该章用 --- 分隔章节，新章节前后也加 ---。

【动作 2】对核心公式（含 ≥3 个陌生符号的定义型公式），在公式上方插入
符号速查表，四列固定：
| 符号 | 名称 | 含义 | 直观解释 |
"直观解释"不超过 15 字，用生活语言。同一符号全章只查一次。

【动作 3】对步骤 ≥3 的关键推导（每章最多 3-5 处）：
- 若推导由多个 $$ 块构成：在块与块之间插入列表项
  「- 【第 k 步】依据：…；目的：…」
- 若是单个长公式块：在块之后追加批注列表，逐个等号解读
$$ 块内容一个字符都不能动。

【动作 4】在全章最末（总结节之后）插入无编号章节「## 常见误区」，
3-5 条，每条格式：
### 误区 N：{一句话错误理解}
**为什么错**：…
**正确理解**：{一句话}
优先覆盖：符号混淆、方向搞反、条件遗漏、类比过度四类。

【格式硬约束】
1. $$ 必须成对，块行首缩进 ≤3 空格；
2. 行内公式用单层 $...$，内部不得再出现 $；
3. 「## 习题」与「## 习题答案与解析」两节绝对不改一个字符；
4. 不改章节编号、不动图片引用、不新增文件、不使用 emoji；
5. 新增类比沿用既有格式「> **直观理解（现实类比）**：」，
   每章最多 3 个，且必须写明类比的失效边界。

【完成后自检并汇报】
1. 四个动作各自的插入位置清单；
2. $$ 计数为偶数、无 4 空格缩进的 $$；
3. 习题两节未被触碰（说明如何确认）；
4. 发现的原文疑似问题清单（只记录，不修改）。
```

---

## 附录 B：完整片段示例（动作二落地效果）

以第 1 级有理数定义为例。

**改写前**（原文，保持不动）：

```markdown
$$
\mathbb{Q} = \left\{\frac{p}{q} \;\middle|\; p, q \in \mathbb{Z},\; q \neq 0 \right\}
$$
```

**改写后**（仅在其上方插入表格，原文未动）：

```markdown
| 符号 | 名称 | 含义 | 直观解释 |
|:---:|:---:|:---|:---|
| $\mathbb{Q}$ | 有理数集 | 全体能写成两整数之比的数 | "能写成分数的数" |
| $p$ | 分子 | 任意整数 | 分子随便取 |
| $q$ | 分母 | 任意非零整数 | 分母不能是 0 |
| $\in$ | 属于 | 左边的元素属于右边集合 | "是……的一员" |
| $\mid$ | 条件分隔 | "其中/满足" | 竖线读作"满足" |

$$
\mathbb{Q} = \left\{\frac{p}{q} \;\middle|\; p, q \in \mathbb{Z},\; q \neq 0 \right\}
$$
```
