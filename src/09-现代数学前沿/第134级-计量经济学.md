# 第 134 级：计量经济学 (Econometrics)

---

## 1. 学习目标

完成本教程后，你将能够：

1. 理解线性回归模型与 OLS 估计的基本框架
2. 掌握 OLS 估计量的一致性和渐近正态性及其证明
3. 理解工具变量（IV）估计的原理与两阶段最小二乘法（2SLS）
4. 掌握广义矩方法（GMM）及其渐近性质
5. 理解面板数据模型（固定效应与随机效应）
6. 掌握差分中的差分（DID）与断点回归（RDD）方法
7. 理解时间序列计量经济学中的协整与误差修正模型
8. 掌握结构方程模型与因果推断的基本思想
9. 理解离散选择模型（Logit、Probit）与计数数据模型

---

## 2. 核心概念

### 2.1 线性回归模型

**线性回归模型**（Linear Regression Model）是计量经济学的基础。标准形式为：

$$
y_i = x_i^\top \beta + \varepsilon_i, \quad i = 1, \dots, n
$$

其中 $y_i$ 为因变量，$x_i \in \mathbb{R}^K$ 为解释变量向量，$\beta \in \mathbb{R}^K$ 为待估参数，$\varepsilon_i$ 为随机误差项。

> **💡 直观理解（类比）**：线性回归就像"用一条直线把散点数据归纳成一句话"——$y_i=x_i^\top\beta+\varepsilon_i$ 说"结果大致由一堆原因加起来决定，外加一点说不清的干扰 $\varepsilon$"。它像配镜师测视力：找一组系数 $\beta$，让"预测的 $y$"尽量贴近"真实的 $y$"，把一群乱糟糟的点，压缩成一条最能代表它们的直线。

**基本假设**：
1. **线性性**：$y = X\beta + \varepsilon$
2. **严格外生性**：$\mathbb{E}[\varepsilon | X] = 0$
3. **球形误差**：$\text{Var}(\varepsilon | X) = \sigma^2 I_n$
4. **满秩**：$\text{rank}(X) = K$

> **💡 直观理解（类比）**：这四个假设是回归的"健康标准"——模型确实是线性的（线性性）、干扰不跟解释变量有任何"勾结"（严格外生性）、干扰彼此独立且声势均匀（球形误差）、自变量之间不重复冗余（满秩）。只要这些"身体指标"正常，OLS 这位"体检医生"给出的结果才可信、才最优。

### 2.2 OLS 估计

**普通最小二乘**（Ordinary Least Squares, OLS）估计量通过最小化残差平方和得到：

$$
\hat{\beta}_{\text{OLS}} = \arg\min_\beta \sum_{i=1}^n (y_i - x_i^\top \beta)^2 = (X^\top X)^{-1} X^\top y
$$

> **💡 直观理解（类比）**：OLS 就是把"误差的平方和"当成惩罚指标，挑出让总偏差最小的那组系数——惩罚平方（而非绝对值）会让大偏差被狠狠当头一棒，所以得到的那条线"宁可均匀地小错，也不愿在某点大错"。$(X^\top X)^{-1}X^\top y$ 就是这道最小化问题的闭式答案，像直接套公式就算出"误差最小王牌"。它的直观画面是：把所有点到直线距离的平方累加，拉来拉去直到总和最小。

### 2.3 工具变量与两阶段最小二乘

当解释变量与误差项相关（内生性）时，OLS 估计不一致。**工具变量**（Instrumental Variables, IV）方法是解决内生性的标准方法。

**工具变量** $z_i$ 需满足：
1. **相关性**：$\text{Cov}(z_i, x_i) \neq 0$
2. **外生性**：$\mathbb{E}[\varepsilon_i | z_i] = 0$

> **💡 直观理解（类比）**：工具变量就是那个"和你熟、却不认识你仇家"的可靠中间人——它必须跟解释变量 $x$ 有交情（相关性，$z$ 确实能撬动 $x$），同时跟误差项 $\varepsilon$ 毫无瓜葛（外生性，$z$ 不掺和干扰因素）。两条缺一不可：相关性不够就是"使不上劲"，外生性被破坏就是"引狼入室"、同样有偏。

**两阶段最小二乘**（Two-Stage Least Squares, 2SLS）：
- 第一阶段：$x_i = z_i^\top \gamma + \eta_i$，得到拟合值 $\hat{x}_i$
- 第二阶段：$y_i = \hat{x}_i^\top \beta + \nu_i$

> **💡 直观理解（类比）**：2SLS 像"洗衣去污分两步"——第一步先用干净的 $z$ 把脏乎乎的 $x$ "滤一遍"，只留下由 $z$ 解释的那部分"净身价" $\hat{x}$（脏的成分被留在残差里）；第二步再用这洗干净的部分 $\hat{x}$ 去回归 $y$。既然 $\hat{x}$ 里已不含与 $\varepsilon$ 纠缠的杂质，估计就清净了。

![内生性与工具变量：z 与 x 相关、与误差项 \varepsilon 外生，从而撬动 x 识别因果效应 \beta](images/134_内生性与工具变量.svg)

> **直观理解（现实类比）**：工具变量就像"借力打力"——当解释变量 $x$ 与误差项 $\varepsilon$ 纠缠不清（内生性）时，OLS 会得到有偏的估计。工具变量 $z$ 好比一个"中间人"：它只影响 $x$（两人是熟人），却和干扰因素 $\varepsilon$ 毫无瓜葛、不直接决定 $y$。于是我们借 $z$ 的"纯变动"来撬动 $x$，就像用一根干净的杠杆避开泥泞，从而看清 $x$ 对 $y$ 真正的因果效应 $\beta$。

> **🧠 思考历程：当"潜在的干扰"混进数据，怎么才能拆出真正的因果？**
>
> 有相关性不等于有因果，可现实数据里解释变量总爱与误差项"勾结"。工具变量正是为这种困境而生的一把"干净杠杆"。它想回答的是：当混淆因素无法直接测量时，还有什么办法能撬出 $x$ 对 $y$ 的真实效应 $\beta$。
>
> - **从遗传学的"路径"说起**。1928年，美国遗传学家休厄尔·赖特在研究遗传与环境对性状的影响时，用"路径系数"第一次表达了工具变量思想：用一个只"触及解释变量"、却和干扰项绝缘的外生变量，来撬动因果。学界常把它与《关税与动植物油脂》一书中对供求曲线的工具变量处理联系在一起。
> - **两阶段回归把想法落地**。1950年代，泰尔（1953）与巴斯曼（1957）把工具变量系统化为两阶段最小二乘（2SLS）：第一阶段用 $z$ 拟合 $x$，第二阶段再拿拟合值去回归 $y$，从而避开 $x$ 与误差项 $\varepsilon$ 的纠缠。
> - **从相关走向因果**。1990年代，安格里斯特与因本斯在"局部处理效应（LATE）"的框架下重新照亮工具变量，让计量经济学真正成为政策评估中拆解因果关系的标准工具。
> - **心路**：数据天生混杂，要看清因果，就得学会"借一把干净的杠杆"——这正是工具变量全部的哲学。

### 2.4 广义矩方法

**广义矩方法**（Generalized Method of Moments, GMM）是 IV 估计的推广。给定矩条件 $\mathbb{E}[g(w_i, \theta)] = 0$，GMM 估计量最小化：

$$
\hat{\theta}_{\text{GMM}} = \arg\min_\theta \bar{g}(\theta)^\top W \bar{g}(\theta)
$$

其中 $\bar{g}(\theta) = \frac{1}{n} \sum_{i=1}^n g(w_i, \theta)$，$W$ 为对称正定权矩阵。

> **💡 直观理解（类比）**：GMM 就是"用一堆'应该平均为零'的等式来反推参数"——矩条件 $\mathbb{E}[g(w_i,\theta)]=0$ 像"数学上理应永远平衡的天平"，把样本里能凑出的一切平衡等式都摆出来。当等式比未知数还多时，就找个参数 $\theta$ 让这些天平"整体都尽量接近零点"，权矩阵 $W$ 则是"给更可靠的天平更高的权重"，所以 GMM 是 IV 思想的通用升级版。

### 2.5 最大似然估计

**最大似然估计**（Maximum Likelihood Estimation, MLE）假设已知 $y$ 的条件分布：

$$
\hat{\beta}_{\text{MLE}} = \arg\max_\beta \sum_{i=1}^n \log f(y_i | x_i, \beta)
$$

> **💡 直观理解（类比）**：MLE 就是"猜哪套参数最像含出我已看到的数据的作者"——把每一点 $y_i$ 在假设分布下的概率 $f(y_i|x_i,\beta)$ 乘起来（取对数把乘变加），再挑让这个"总概率"最大的 $\beta$。它像断案：谁最能让眼前的证据（数据）看起来顺理成章，就认谁是凶手。对正态误差时它和 OLS 殊途同归。

对于正态误差的线性模型，MLE 与 OLS 等价。

### 2.6 面板数据模型

**面板数据**（Panel Data）包含多个个体在不同时间点的观测值。

> **💡 直观理解（类比）**：面板数据就像"长期追踪一群人的跟踪表"——不光记录某一天谁高谁矮（横截面），而是每隔一段时间给同一批人量一次（时间纵轴），组成一张"个子、体重随时间变化"的表格。有了这样"既看人多、又看时间久"的数据，才能同时捕捉个体之间与时间前后的变化。

**固定效应模型**（Fixed Effects）：
$$
y_{it} = x_{it}^\top \beta + \alpha_i + \varepsilon_{it}
$$

其中 $\alpha_i$ 为个体固定效应，允许与 $x_{it}$ 相关。

> **💡 直观理解（类比）**：固定效应就像"给每个人发一个专属身份标签"——每个人天生那点不变的"体质/性格/运气"用一个 $\alpha_i$ 记着，且它可以跟解释变量有牵扯（比如爱学习的人本身就聪明）。我们靠"同一个人前后比"把这些固定标签自动抵消掉，从而只看真正随时间和投入变的因素。

**随机效应模型**（Random Effects）：
$$
y_{it} = x_{it}^\top \beta + \alpha_i + \varepsilon_{it}
$$

其中 $\alpha_i \sim (0, \sigma_\alpha^2)$ 且与 $x_{it}$ 不相关。

> **💡 直观理解（类比）**：随机效应装作那些"个体差异 $\alpha_i$"是独立随机抽的、跟 $x$ 没任何往来（像彩票，跟你的投入无关），因此能更省力地把多种变异一起用起来，得到更高效率。但一旦 $\alpha_i$ 其实和 $x$ 有关系，这种"不相关"的侥幸就被戳穿、结果偏掉了——所以相关时只能用固定效应。

### 2.7 差分中的差分

**差分中的差分**（Difference-in-Differences, DID）用于评估政策处理效应。比较处理组和对照组在处理前后的变化：

$$
\hat{\tau}_{\text{DID}} = (\bar{y}_{\text{处理,后}} - \bar{y}_{\text{处理,前}}) - (\bar{y}_{\text{对照,后}} - \bar{y}_{\text{对照,前}})
$$

> **💡 直观理解（类比）**：DID 就是把两组前后变化的**差额再减一次**——先算处理组"政策前后差了多少"，再算对照组"这段时间自然变了多少"，两者相减，剩下的才是政策"额外"的功劳。它像"看电视遥控比信号"：对照组是没装天线的邻居家，处理组装天线后图像更好；把邻居自然的变化扣掉，剩下的增益才是天线真正带来的。

**平行趋势假设**（Parallel Trends Assumption）是 DID 的关键识别条件。

> **💡 直观理解（类比）**：平行趋势假设是 DID 的"命根子"——它要求"如果没有政策，处理组和对照组本该走得一样齐"。就像赛跑前要先确认两条跑道平时的人流量一样：要是本来两条道就一个热一个冷、会越差越多，那你减出来的"差距"就分不清是政策还是本来就有的趋势偏差了。前提不牢，结论全塌。

### 2.8 断点回归

**断点回归**（Regression Discontinuity Design, RDD）利用处理变量在阈值的跳跃识别因果效应。当分配变量 $x_i$ 超过阈值 $c$ 时接受处理：

$$
\tau_{\text{RDD}} = \lim_{x \downarrow c} \mathbb{E}[y | x] - \lim_{x \uparrow c} \mathbb{E}[y | x]
$$

> **💡 直观理解（类比）**：断点回归就像"只盯着门槛两边差那口气的细微比较"——比如60分及格线：考59.9和60.1的人，能力几乎没差，可就因为差这一丁点、一个合格一个不合格，于是把这两拨"差点左右的人"的结果一对比，那点跳跃就是及格线（处理）的纯因果效应。它用"恰好卡在门槛"这几分公平对决，绕开了"能上线的本来就强"的自选择问题。

### 2.9 时间序列计量经济学

**协整**（Cointegration）：若 $y_t$ 和 $x_t$ 均为 $I(1)$ 过程，但存在线性组合 $y_t - \beta x_t \sim I(0)$，则称二者协整。

![平稳 y_t=\rho y_{t-1}+\varepsilon_t（|\rho|<1）围绕均值回归，vs 非平稳/单位根 y_t=y_{t-1}+\varepsilon_t（\rho=1）随机游走、方差发散](images/134_平稳与非平稳时间序列.svg)

> **直观理解（现实类比）**：平稳序列像"被弹簧拴住的小球"——无论怎么被随机踢打，总会荡回均值附近来回摆动；而非平稳序列（单位根）则像"喝醉的旅人"——每一步的随机位移都累积起来，越走越远、毫无回归意思。正因如此，对非平稳数据直接回归会产生"看似相关实则无关"的伪回归，所以先检验单位根（ADF检验）、再用协整与误差修正模型，才能得到可信的关系。

**误差修正模型**（Error Correction Model, ECM）：
$$
\Delta y_t = \alpha(y_{t-1} - \beta x_{t-1}) + \gamma \Delta x_t + \varepsilon_t
$$

> **💡 直观理解（类比）**：ECM 就像给"会走散的二人组"安装了一根弹性橡皮筋——$y$ 和 $x$ 长期该保持固定比例 $y_t=\beta x_t$，一旦暂时跑偏（$y_{t-1}-\beta x_{t-1}\neq0$），下一期就会被系数 $\alpha<0$ 的"回拉力"拽回正轨。它同时记录"短期随 $x$ 动的 $\gamma\Delta x_t$"和"长期纠偏的 $\alpha(\cdot)$"，把"近视的日常波动"和"远视的均衡回归"都讲明白了。

**向量自回归**（Vector Autoregression, VAR）：
$$
y_t = A_1 y_{t-1} + \cdots + A_p y_{t-p} + \varepsilon_t
$$

> **💡 直观理解（类比）**：VAR 就是"让一堆变量彼此互相看昨天脸色"——$y_t$ 不只由自己，还由其他变量的过去共同决定（矩阵 $A_k$ 管着相互牵动），且不加任何"某变量固定归谁管"的条条框框。它像一帮互相影响的老友：谁昨天怎样，会影响别人今天怎样，全放在一起估计，体现"万物皆相关"的密集动态网。

### 2.10 结构方程模型

**结构方程模型**（Structural Equation Modeling, SEM）结合因子分析和路径分析，处理潜变量和测量误差。

> **💡 直观理解（类比）**：SEM 就是"给看不见的'潜变量'装上可测量的探针，再画出因果路径图"——有些概念（如智力、幸福感）无法直接量，但能通过好几个能测的指标间接捕捉，SEM 同时处理"测不测准"（测量模型）和"谁影响谁"（结构路径），像给一台复杂机械既画零件图、又画动力传递图。

### 2.11 离散选择模型

**Logit 模型**：
$$
P(y_i = 1 | x_i) = \frac{\exp(x_i^\top \beta)}{1 + \exp(x_i^\top \beta)}
$$

**Probit 模型**：
$$
P(y_i = 1 | x_i) = \Phi(x_i^\top \beta)
$$

其中 $\Phi(\cdot)$ 为标准正态分布函数。

> **💡 直观理解（类比）**：Logit 和 Probit 都是"把'做与不做'的二选一变成一条平滑概率曲线"——$x$ 越强，$P(y=1)$ 越高，但被夹在0和1之间、永远不超过边界。两者的差别只是"累计算概率用的曲线形状"：Logit 用逻辑斯蒂$S$形曲线、Probit 用正态累积曲线，长得几乎一样。它们取代线性概率模型，避免了"算出来的概率大于1或小于0"的尴尬，专用于是非、投保、买房这类0/1决策。

### 2.12 计数数据模型

**泊松回归**（Poisson Regression）：
$$
P(y_i = k | x_i) = \frac{\lambda_i^k e^{-\lambda_i}}{k!}, \quad \lambda_i = \exp(x_i^\top \beta)
$$

> **💡 直观理解（类比）**：泊松回归专管"数数类"的结果——事故数、就诊次数、打进的电话个数这种非负整数。它假设结果按速率 $\lambda_i$ 随机生事，且速率取对数后是 $x$ 的线性函数。它像"计数门卫"：$x$ 提高就调快"出事频率 $\lambda_i$"，从而影响平均每期会发生几件。

### 2.13 因果推断

**因果推断**（Causal Inference）使用潜在结果框架（Rubin Causal Model）：对每个个体 $i$，定义潜在结果 $Y_i(1)$（处理）和 $Y_i(0)$（对照）。个体处理效应为 $\tau_i = Y_i(1) - Y_i(0)$。

> **💡 直观理解（类比）**：潜在结果框架是"借一个人'一体两面的命运'来讲因果"——同一个 $i$，既设想他吃了药的结果 $Y_i(1)$、又设想他没吃药的结果 $Y_i(0)$，差值就是"这粒药对他有多大的因果作用"。但现实里每人只能亲身经历其中一种，另一面是看不到的"平行世界"。所以因果推断的毕生追求，就是用对照、随机化等手段，尽量"造出"那个看不见的反事实。

---

## 3. 定理与证明

### 定理 3.1：OLS 估计量的一致性和渐近正态性

**定理**：考虑线性回归模型 $y_i = x_i^\top \beta_0 + \varepsilon_i$，其中 $\{\varepsilon_i\}$ 为鞅差序列（martingale difference sequence），满足 $\mathbb{E}[\varepsilon_i | \mathcal{F}_{i-1}] = 0$，$\mathbb{E}[\varepsilon_i^2 | \mathcal{F}_{i-1}] = \sigma^2$。假设 $n^{-1} \sum_{i=1}^n x_i x_i^\top \xrightarrow{p} Q$（正定矩阵），且存在 $\delta > 0$ 使得 $\sup_i \mathbb{E}[|\varepsilon_i|^{2+\delta} | \mathcal{F}_{i-1}] < \infty$。则：

1. **一致性**：$\hat{\beta}_{\text{OLS}} \xrightarrow{p} \beta_0$
2. **渐近正态性**：$\sqrt{n}(\hat{\beta}_{\text{OLS}} - \beta_0) \xrightarrow{d} N(0, \sigma^2 Q^{-1})$

> **💡 直观理解（类比）**：这个定理给了 OLS 两张"成人大礼包"——**一致性**说"数据越多，估计就越收敛真的 $\beta_0$"（像称体重，样本越大越准）；**渐近正态性**说"大样本下估计误差围绕真值形成钟形分布"（像射击散布，绝大多数都落在正中央附近）。它告诉我们样本够大时不仅能信 OLS，还能量出"误差有多大"（$\sigma^2Q^{-1}$），为假设检验和置信区间打地基。

**证明**：

**步骤1：OLS 估计量的表达式**

$$
\hat{\beta}_{\text{OLS}} = \left(\sum_{i=1}^n x_i x_i^\top\right)^{-1} \sum_{i=1}^n x_i y_i = \beta_0 + \left(\sum_{i=1}^n x_i x_i^\top\right)^{-1} \sum_{i=1}^n x_i \varepsilon_i
$$

因此：

$$
\hat{\beta}_{\text{OLS}} - \beta_0 = \left(\sum_{i=1}^n x_i x_i^\top\right)^{-1} \sum_{i=1}^n x_i \varepsilon_i
$$

**步骤2：一致性证明**

令 $S_n = \sum_{i=1}^n x_i x_i^\top$。由假设，$n^{-1} S_n \xrightarrow{p} Q$，因此 $S_n^{-1} = O_p(n^{-1})$。

考虑 $\frac{1}{n} \sum_{i=1}^n x_i \varepsilon_i$。由于 $\mathbb{E}[x_i \varepsilon_i] = \mathbb{E}[x_i \mathbb{E}[\varepsilon_i | \mathcal{F}_{i-1}]] = 0$，且 $\text{Var}\left(\frac{1}{n} \sum_{i=1}^n x_i \varepsilon_i\right) = \frac{1}{n^2} \sum_{i=1}^n \mathbb{E}[x_i x_i^\top \varepsilon_i^2] = \frac{\sigma^2}{n^2} \sum_{i=1}^n \mathbb{E}[x_i x_i^\top] = O(1/n)$。

由大数定律，$\frac{1}{n} \sum_{i=1}^n x_i \varepsilon_i \xrightarrow{p} 0$。因此：

$$
\hat{\beta}_{\text{OLS}} - \beta_0 = S_n^{-1} \cdot \sum_{i=1}^n x_i \varepsilon_i = \left(\frac{S_n}{n}\right)^{-1} \cdot \frac{1}{n} \sum_{i=1}^n x_i \varepsilon_i \xrightarrow{p} Q^{-1} \cdot 0 = 0
$$

故 $\hat{\beta}_{\text{OLS}} \xrightarrow{p} \beta_0$。

**步骤3：渐近正态性证明**

令 $Z_n = \frac{1}{\sqrt{n}} \sum_{i=1}^n x_i \varepsilon_i$。定义 $\xi_{ni} = \frac{1}{\sqrt{n}} x_i \varepsilon_i$，$i = 1, \dots, n$。

则 $\{\xi_{ni}\}$ 关于滤子 $\{\mathcal{F}_{i-1}\}$ 构成一个鞅差阵列（martingale difference array），因为：

$$
\mathbb{E}[\xi_{ni} | \mathcal{F}_{i-1}] = \frac{1}{\sqrt{n}} x_i \mathbb{E}[\varepsilon_i | \mathcal{F}_{i-1}] = 0
$$

**步骤4：鞅差中心极限定理的应用**

应用鞅差中心极限定理（Martingale CLT），需要验证两个条件：

(i) **条件方差收敛**：

$$
\sum_{i=1}^n \mathbb{E}[\xi_{ni} \xi_{ni}^\top | \mathcal{F}_{i-1}] = \frac{1}{n} \sum_{i=1}^n x_i x_i^\top \mathbb{E}[\varepsilon_i^2 | \mathcal{F}_{i-1}] \xrightarrow{p} \sigma^2 Q
$$

(ii) **Lindeberg 条件**：对任意 $\delta > 0$，

$$
\sum_{i=1}^n \mathbb{E}[\|\xi_{ni}\|^2 \mathbf{1}_{\{\|\xi_{ni}\| > \delta\}} | \mathcal{F}_{i-1}] \xrightarrow{p} 0
$$

由 $\sup_i \mathbb{E}[|\varepsilon_i|^{2+\delta} | \mathcal{F}_{i-1}] < \infty$，利用 Hölder 不等式可证 Lindeberg 条件成立。

**步骤5：鞅差 CLT 结论**

由鞅差 CLT：

$$
Z_n = \frac{1}{\sqrt{n}} \sum_{i=1}^n x_i \varepsilon_i \xrightarrow{d} N(0, \sigma^2 Q)
$$

**步骤6：Delta 方法**

由于 $\hat{\beta}_{\text{OLS}} - \beta_0 = (S_n/n)^{-1} \cdot (1/n) \sum_{i=1}^n x_i \varepsilon_i$，且 $S_n/n \xrightarrow{p} Q$，使用 Slutsky 定理：

$$
\sqrt{n}(\hat{\beta}_{\text{OLS}} - \beta_0) = \left(\frac{S_n}{n}\right)^{-1} \cdot \frac{1}{\sqrt{n}} \sum_{i=1}^n x_i \varepsilon_i \xrightarrow{d} Q^{-1} \cdot N(0, \sigma^2 Q) = N(0, \sigma^2 Q^{-1})
$$

**步骤7：结论**

因此，$\hat{\beta}_{\text{OLS}}$ 是 $\beta_0$ 的一致估计，且渐近服从正态分布 $N(0, \sigma^2 Q^{-1}/n)$。$\square$

---

### 定理 3.2：工具变量估计的一致性

**定理**：考虑线性模型 $y_i = x_i^\top \beta_0 + \varepsilon_i$，其中 $x_i$ 可能与 $\varepsilon_i$ 相关（内生性）。设 $z_i \in \mathbb{R}^L$（$L \ge K$）为工具变量，满足：

1. **相关性**：$\mathbb{E}[z_i x_i^\top]$ 满秩（秩 $K$）
2. **外生性**：$\mathbb{E}[z_i \varepsilon_i] = 0$

则 IV 估计量 $\hat{\beta}_{\text{IV}} = (Z^\top X)^{-1} Z^\top y$（恰好识别时）是 $\beta_0$ 的一致估计。

**证明**：

**步骤1：IV 估计量的表达式**

代入 $y = X\beta_0 + \varepsilon$：

$$
\hat{\beta}_{\text{IV}} = (Z^\top X)^{-1} Z^\top (X\beta_0 + \varepsilon) = \beta_0 + (Z^\top X)^{-1} Z^\top \varepsilon
$$

因此：

$$
\hat{\beta}_{\text{IV}} - \beta_0 = (Z^\top X)^{-1} Z^\top \varepsilon
$$

**步骤2：正交条件（矩条件）**

由外生性假设 $\mathbb{E}[z_i \varepsilon_i] = 0$，这是矩条件（moment condition）的核心：

$$
\mathbb{E}[z_i (y_i - x_i^\top \beta_0)] = 0
$$

IV 估计量正是通过求解样本矩条件得到的：

$$
\frac{1}{n} \sum_{i=1}^n z_i (y_i - x_i^\top \hat{\beta}_{\text{IV}}) = 0
$$

**步骤3：一致性证明**

将 $\hat{\beta}_{\text{IV}} - \beta_0$ 写为：

$$
\hat{\beta}_{\text{IV}} - \beta_0 = \left(\frac{1}{n} Z^\top X\right)^{-1} \cdot \frac{1}{n} Z^\top \varepsilon
$$

由大数定律：

$$
\frac{1}{n} Z^\top X = \frac{1}{n} \sum_{i=1}^n z_i x_i^\top \xrightarrow{p} \mathbb{E}[z_i x_i^\top] \equiv \Sigma_{zx}
$$

由相关性假设，$\Sigma_{zx}$ 满秩，因此可逆。

同样，由外生性假设和大数定律：

$$
\frac{1}{n} Z^\top \varepsilon = \frac{1}{n} \sum_{i=1}^n z_i \varepsilon_i \xrightarrow{p} \mathbb{E}[z_i \varepsilon_i] = 0
$$

因此：

$$
\hat{\beta}_{\text{IV}} - \beta_0 \xrightarrow{p} \Sigma_{zx}^{-1} \cdot 0 = 0
$$

故 $\hat{\beta}_{\text{IV}} \xrightarrow{p} \beta_0$。

**步骤4：关于过度识别**

当 $L > K$（过度识别）时，2SLS 估计量为：

$$
\hat{\beta}_{\text{2SLS}} = (X^\top Z (Z^\top Z)^{-1} Z^\top X)^{-1} X^\top Z (Z^\top Z)^{-1} Z^\top y
$$

这是所有线性组合 $\hat{X} = Z(Z^\top Z)^{-1} Z^\top X$ 中最佳的 IV 估计量。其一致性证明类似，通过将 $X$ 投影到 $Z$ 张成的空间上。$\square$
> **直观理解（现实类比）**：内生性就像"因果被搅浑的汤"——你想测"学习时长"对"成绩"的影响，可偏偏爱学习的学生本身就更聪明、更自律（这些没量到的因素混在误差项里），于是 OLS 把"聪明"的功劳错记到"学习时长"头上。自变量和误差项一勾结，估计就偏了。正因如此，计量经济学才要大费周章找工具变量、做实验设计，把"搅汤的勺子"捞出来。

---

### 定理 3.3：GMM 估计量的渐近性质

**定理**：设 $\theta_0$ 为 $K$ 维参数向量，满足矩条件 $\mathbb{E}[g(w_i, \theta_0)] = 0$，其中 $g(\cdot, \cdot) \in \mathbb{R}^L$（$L \ge K$）。GMM 估计量定义为：

$$
\hat{\theta}_{\text{GMM}} = \arg\min_\theta \bar{g}(\theta)^\top \hat{W} \bar{g}(\theta)
$$

其中 $\bar{g}(\theta) = \frac{1}{n} \sum_{i=1}^n g(w_i, \theta)$，$\hat{W} \xrightarrow{p} W$（$W$ 为正定对称矩阵）。在正则条件下，GMM 估计量满足：

1. **一致性**：$\hat{\theta}_{\text{GMM}} \xrightarrow{p} \theta_0$
2. **渐近正态性**：$\sqrt{n}(\hat{\theta}_{\text{GMM}} - \theta_0) \xrightarrow{d} N(0, (G^\top W G)^{-1} G^\top W S W G (G^\top W G)^{-1})$
3. **最优权矩阵**：当 $W = S^{-1}$ 时（$S = \text{Var}(\sqrt{n} \bar{g}(\theta_0))$），渐近方差最小，为 $(G^\top S^{-1} G)^{-1}$

其中 $G = \mathbb{E}[\partial g(w_i, \theta_0)/\partial \theta^\top]$，$S = \lim_{n \to \infty} \text{Var}(\sqrt{n} \bar{g}(\theta_0))$。

> **💡 直观理解（类比）**：GMM 定理给出了用"平衡等式"反推参数时的三道保障——大样本下估计真诚地收敛（一致性）、误差呈钟形分布（渐近正态性）、而当把每个平衡等式按它的"可信度方差 $S$"来加权（取 $W=S^{-1}$）时，估计最准（最优权矩阵）。它像给天平配重：用得恰到好处的砝码，能让测量结果既稳又精。

**证明**：

**步骤1：一致性证明**

定义 $Q_n(\theta) = \bar{g}(\theta)^\top \hat{W} \bar{g}(\theta)$，$Q_\infty(\theta) = \mathbb{E}[g(w_i, \theta)]^\top W \mathbb{E}[g(w_i, \theta)]$。

由大数定律，$\bar{g}(\theta) \xrightarrow{p} \mathbb{E}[g(w_i, \theta)]$ 对每个 $\theta$ 成立。由 $\hat{W} \xrightarrow{p} W$，对每个 $\theta$ 有 $Q_n(\theta) \xrightarrow{p} Q_\infty(\theta)$。

由矩条件，$\mathbb{E}[g(w_i, \theta_0)] = 0$，因此 $Q_\infty(\theta_0) = 0$。由识别条件，对任意 $\theta \neq \theta_0$，$\mathbb{E}[g(w_i, \theta)] \neq 0$，因此 $Q_\infty(\theta) > 0$。

由一致收敛性（通过随机等度连续性）和 $Q_\infty$ 在 $\theta_0$ 处有唯一最小值，可得 $\hat{\theta}_{\text{GMM}} \xrightarrow{p} \theta_0$。

**步骤2：一阶条件**

GMM 估计量的一阶条件为：

$$
\frac{\partial Q_n(\hat{\theta})}{\partial \theta} = 2 \hat{G}(\hat{\theta})^\top \hat{W} \bar{g}(\hat{\theta}) = 0
$$

其中 $\hat{G}(\theta) = \frac{1}{n} \sum_{i=1}^n \frac{\partial g(w_i, \theta)}{\partial \theta^\top}$。

**步骤3：均值展开**

对 $\bar{g}(\hat{\theta})$ 在 $\theta_0$ 处进行一阶泰勒展开：

$$
\bar{g}(\hat{\theta}) = \bar{g}(\theta_0) + \hat{G}(\bar{\theta})(\hat{\theta} - \theta_0)
$$

其中 $\bar{\theta}$ 在 $\hat{\theta}$ 和 $\theta_0$ 之间。

代入一阶条件：

$$
\hat{G}(\hat{\theta})^\top \hat{W} [\bar{g}(\theta_0) + \hat{G}(\bar{\theta})(\hat{\theta} - \theta_0)] = 0
$$

**步骤4：求解 $\hat{\theta} - \theta_0$**

整理得：

$$
\hat{G}(\hat{\theta})^\top \hat{W} \hat{G}(\bar{\theta})(\hat{\theta} - \theta_0) = -\hat{G}(\hat{\theta})^\top \hat{W} \bar{g}(\theta_0)
$$

因此：

$$
\sqrt{n}(\hat{\theta} - \theta_0) = -[\hat{G}(\hat{\theta})^\top \hat{W} \hat{G}(\bar{\theta})]^{-1} \hat{G}(\hat{\theta})^\top \hat{W} \sqrt{n} \bar{g}(\theta_0)
$$

**步骤5：渐近正态性**

由一致性，$\hat{G}(\hat{\theta}) \xrightarrow{p} G$，$\hat{G}(\bar{\theta}) \xrightarrow{p} G$。由中心极限定理：

$$
\sqrt{n} \bar{g}(\theta_0) = \frac{1}{\sqrt{n}} \sum_{i=1}^n g(w_i, \theta_0) \xrightarrow{d} N(0, S)
$$

其中 $S = \lim_{n \to \infty} \text{Var}(\sqrt{n} \bar{g}(\theta_0))$。

由 Slutsky 定理：

$$
\sqrt{n}(\hat{\theta} - \theta_0) \xrightarrow{d} -(G^\top W G)^{-1} G^\top W \cdot N(0, S) = N(0, (G^\top W G)^{-1} G^\top W S W G (G^\top W G)^{-1})
$$

**步骤6：最优权矩阵**

选择 $W$ 最小化渐近方差 $V(W) = (G^\top W G)^{-1} G^\top W S W G (G^\top W G)^{-1}$。

这是一个矩阵优化问题。可以证明，$V(W) \ge (G^\top S^{-1} G)^{-1}$（在矩阵正定意义下），等号当且仅当 $W \propto S^{-1}$ 时成立。

**证明概要**：令 $A = S^{1/2} G (G^\top S^{-1} G)^{-1}$，$B = S^{1/2} W G (G^\top W G)^{-1}$。则 $V(W) = B^\top B$，且 $A^\top A = (G^\top S^{-1} G)^{-1}$。由 Cauchy-Schwarz 不等式，$B^\top B \ge (A^\top B)^\top (A^\top A)^{-1} (A^\top B)$。计算 $A^\top B = (G^\top S^{-1} G)^{-1} G^\top S^{-1} S^{1/2} \cdot S^{1/2} W G (G^\top W G)^{-1} = I$，因此 $V(W) \ge (G^\top S^{-1} G)^{-1}$。

**步骤7：最优 GMM**

使用 $W = \hat{S}^{-1}$（其中 $\hat{S}$ 是 $S$ 的一致估计），得到最优 GMM 估计量：

$$
\hat{\theta}_{\text{OGMM}} = \arg\min_\theta \bar{g}(\theta)^\top \hat{S}^{-1} \bar{g}(\theta)
$$

其渐近分布为：

$$
\sqrt{n}(\hat{\theta}_{\text{OGMM}} - \theta_0) \xrightarrow{d} N(0, (G^\top S^{-1} G)^{-1})
$$

$\square$

---

### 定理 3.4：面板数据固定效应估计的一致性

**定理**：考虑固定效应面板数据模型：

$$
y_{it} = x_{it}^\top \beta + \alpha_i + \varepsilon_{it}, \quad i = 1, \dots, N, \; t = 1, \dots, T
$$

其中 $\alpha_i$ 为个体固定效应，允许与 $x_{it}$ 任意相关。固定效应（组内）估计量 $\hat{\beta}_{\text{FE}}$ 通过对每个个体进行组内变换（within transformation）后应用 OLS 得到。在标准假设下（$N \to \infty$，$T$ 固定），$\hat{\beta}_{\text{FE}}$ 是 $\beta$ 的一致估计。

**证明**：

**步骤1：组内变换**

对每个个体 $i$，定义组内均值 $\bar{y}_i = \frac{1}{T} \sum_{t=1}^T y_{it}$，$\bar{x}_i = \frac{1}{T} \sum_{t=1}^T x_{it}$，$\bar{\varepsilon}_i = \frac{1}{T} \sum_{t=1}^T \varepsilon_{it}$。

对原始方程取组内均值：

$$
\bar{y}_i = \bar{x}_i^\top \beta + \alpha_i + \bar{\varepsilon}_i
$$

从原始方程减去组内均值方程（消去 $\alpha_i$）：

$$
y_{it} - \bar{y}_i = (x_{it} - \bar{x}_i)^\top \beta + (\varepsilon_{it} - \bar{\varepsilon}_i)
$$

记 $\ddot{y}_{it} = y_{it} - \bar{y}_i$，$\ddot{x}_{it} = x_{it} - \bar{x}_i$，$\ddot{\varepsilon}_{it} = \varepsilon_{it} - \bar{\varepsilon}_i$，则：

$$
\ddot{y}_{it} = \ddot{x}_{it}^\top \beta + \ddot{\varepsilon}_{it}
$$

**步骤2：固定效应估计量**

固定效应估计量就是对变换后的方程应用 OLS：

$$
\hat{\beta}_{\text{FE}} = \left(\sum_{i=1}^N \sum_{t=1}^T \ddot{x}_{it} \ddot{x}_{it}^\top\right)^{-1} \sum_{i=1}^N \sum_{t=1}^T \ddot{x}_{it} \ddot{y}_{it}
$$

代入 $\ddot{y}_{it}$：

$$
\hat{\beta}_{\text{FE}} = \beta + \left(\sum_{i=1}^N \sum_{t=1}^T \ddot{x}_{it} \ddot{x}_{it}^\top\right)^{-1} \sum_{i=1}^N \sum_{t=1}^T \ddot{x}_{it} \ddot{\varepsilon}_{it}
$$

**步骤3：一致性证明的关键**

一致性要求 $\mathbb{E}[\ddot{x}_{it} \ddot{\varepsilon}_{it}] = 0$。注意：

$$
\ddot{\varepsilon}_{it} = \varepsilon_{it} - \frac{1}{T} \sum_{s=1}^T \varepsilon_{is}
$$

由于 $\ddot{x}_{it}$ 包含所有时期的 $x_{is}$（通过 $\bar{x}_i$），严格外生性假设 $\mathbb{E}[\varepsilon_{it} | x_{i1}, \dots, x_{iT}] = 0$ 是必要的。在此假设下：

$$
\mathbb{E}[\ddot{x}_{it} \ddot{\varepsilon}_{it}] = \mathbb{E}[\ddot{x}_{it} \varepsilon_{it}] - \frac{1}{T} \sum_{s=1}^T \mathbb{E}[\ddot{x}_{it} \varepsilon_{is}] = 0
$$

**步骤4：大数定律的应用**

令 $S_{NT} = \frac{1}{NT} \sum_{i=1}^N \sum_{t=1}^T \ddot{x}_{it} \ddot{x}_{it}^\top$。由大数定律，当 $N \to \infty$ 时：

$$
S_{NT} \xrightarrow{p} \mathbb{E}[\ddot{x}_{it} \ddot{x}_{it}^\top] \equiv Q_{\ddot{x}}
$$

只要 $Q_{\ddot{x}}$ 正定（即 $\ddot{x}_{it}$ 存在足够的组内变异），$S_{NT}^{-1} = O_p(1)$。

同样，$\frac{1}{NT} \sum_{i=1}^N \sum_{t=1}^T \ddot{x}_{it} \ddot{\varepsilon}_{it} \xrightarrow{p} \mathbb{E}[\ddot{x}_{it} \ddot{\varepsilon}_{it}] = 0$。

**步骤5：结论**

因此：

$$
\hat{\beta}_{\text{FE}} - \beta = S_{NT}^{-1} \cdot \frac{1}{NT} \sum_{i=1}^N \sum_{t=1}^T \ddot{x}_{it} \ddot{\varepsilon}_{it} \xrightarrow{p} Q_{\ddot{x}}^{-1} \cdot 0 = 0
$$

故 $\hat{\beta}_{\text{FE}} \xrightarrow{p} \beta$。

**步骤6：关于 $T$ 固定的讨论**

当 $T$ 固定而 $N \to \infty$ 时，每个个体的组内变换引入了 $\ddot{\varepsilon}_{it} = \varepsilon_{it} - \bar{\varepsilon}_i$，导致 $\ddot{\varepsilon}_{it}$ 在 $t$ 维度上存在序列相关性（因为 $\bar{\varepsilon}_i$ 依赖所有 $t$ 的 $\varepsilon_{it}$）。这虽然不影响一致性，但会影响标准误的估计，需要使用聚类标准误（clustered standard errors）进行校正。$\square$
> **直观理解（现实类比）**：面板数据固定效应就像"同一个人前后比"——每个人都是自己的对照组，先把个体固有的"脾气禀赋"减掉（组内变换），剩下的差异就干净了。比如研究"加班"对"健康"的影响，张三天生爱熬夜又体弱，直接横截面比会被"张三体质"带偏；固定效应盯住张三自己：他加班多的月份 vs 加班少的月份，就把"体质"这个不随时间变的干扰消掉了。

---

### 定理 3.5：DID 估计量的识别条件

**定理**：考虑两期两组的 DID 设计。设 $Y_{it}(1)$ 和 $Y_{it}(0)$ 分别为个体 $i$ 在时间 $t$ 接受处理和不接受处理的潜在结果。令 $D_i \in \{0, 1\}$ 为处理组指示变量，$T_t \in \{0, 1\}$ 为时间指示变量（$T_t = 1$ 表示处理后时期）。观测结果为 $Y_{it} = D_i T_t Y_{it}(1) + (1 - D_i T_t) Y_{it}(0)$。

DID 估计量定义为：

$$
\hat{\tau}_{\text{DID}} = (\bar{Y}_{11} - \bar{Y}_{10}) - (\bar{Y}_{01} - \bar{Y}_{00})
$$

其中 $\bar{Y}_{dt} = \frac{1}{N_d} \sum_{i: D_i = d} Y_{it}$ 为组 $d$ 在时间 $t$ 的样本均值。

在平行趋势假设下，$\hat{\tau}_{\text{DID}}$ 是平均处理效应（ATT）的一致估计。

**证明**：

**步骤1：潜在结果框架**

定义处理组在时间 $t$ 的平均潜在结果：

- $Y_{t}(1) = \mathbb{E}[Y_{it}(1) | D_i = 1]$（处理组接受处理时的平均结果）
- $Y_{t}(0) = \mathbb{E}[Y_{it}(0) | D_i = 1]$（处理组未接受处理时的平均结果，反事实）

处理组在时间 $t$ 的平均处理效应为 $\tau_t = Y_t(1) - Y_t(0)$。

我们关注的处理效应为 ATT（Average Treatment Effect on the Treated）：

$$
\tau_{\text{ATT}} = \mathbb{E}[Y_{i1}(1) - Y_{i1}(0) | D_i = 1]
$$

**步骤2：观测均值的期望**

对于处理组，在 $t=0$（处理前），观测结果等于 $Y_{it}(0)$：

$$
\mathbb{E}[\bar{Y}_{10}] = \mathbb{E}[Y_{i0}(0) | D_i = 1] = Y_0(0)
$$

在 $t=1$（处理后），观测结果等于 $Y_{it}(1)$：

$$
\mathbb{E}[\bar{Y}_{11}] = \mathbb{E}[Y_{i1}(1) | D_i = 1] = Y_1(1)
$$

对于对照组，所有时期观测结果均等于 $Y_{it}(0)$：

$$
\mathbb{E}[\bar{Y}_{00}] = \mathbb{E}[Y_{i0}(0) | D_i = 0], \quad \mathbb{E}[\bar{Y}_{01}] = \mathbb{E}[Y_{i1}(0) | D_i = 0]
$$

**步骤3：DID 估计量的期望**

$$
\begin{aligned}
\mathbb{E}[\hat{\tau}_{\text{DID}}] &= (\mathbb{E}[\bar{Y}_{11}] - \mathbb{E}[\bar{Y}_{10}]) - (\mathbb{E}[\bar{Y}_{01}] - \mathbb{E}[\bar{Y}_{00}]) \\
&= (Y_1(1) - Y_0(0)) - (\mathbb{E}[Y_{i1}(0) | D_i = 0] - \mathbb{E}[Y_{i0}(0) | D_i = 0])
\end{aligned}
$$

**步骤4：平行趋势假设**

**平行趋势假设**（Parallel Trends Assumption）：处理组和对照组在没有处理的情况下，结果变量的变化趋势相同。即：

$$
\mathbb{E}[Y_{i1}(0) | D_i = 1] - \mathbb{E}[Y_{i0}(0) | D_i = 1] = \mathbb{E}[Y_{i1}(0) | D_i = 0] - \mathbb{E}[Y_{i0}(0) | D_i = 0]
$$

换言之，处理组和对照组的反事实变化率相同。

**步骤5：识别 ATT**

在平行趋势假设下，代入 $\mathbb{E}[\hat{\tau}_{\text{DID}}]$：

$$
\begin{aligned}
\mathbb{E}[\hat{\tau}_{\text{DID}}] &= (Y_1(1) - Y_0(0)) - (\mathbb{E}[Y_{i1}(0) | D_i = 0] - \mathbb{E}[Y_{i0}(0) | D_i = 0]) \\
&= (Y_1(1) - Y_0(0)) - (\mathbb{E}[Y_{i1}(0) | D_i = 1] - \mathbb{E}[Y_{i0}(0) | D_i = 1]) \quad (\text{由平行趋势}) \\
&= (Y_1(1) - \mathbb{E}[Y_{i1}(0) | D_i = 1]) - (Y_0(0) - \mathbb{E}[Y_{i0}(0) | D_i = 1]) \\
&= \tau_1 - \tau_0
\end{aligned}
$$

由于在 $t=0$ 时（处理前），$Y_{i0}(1) = Y_{i0}(0)$（尚未接受处理），因此 $\tau_0 = 0$。故：

$$
\mathbb{E}[\hat{\tau}_{\text{DID}}] = \tau_1 = \mathbb{E}[Y_{i1}(1) - Y_{i1}(0) | D_i = 1] = \tau_{\text{ATT}}
$$

**步骤6：一致性**

由大数定律，样本均值收敛到总体均值：

$$
\bar{Y}_{dt} \xrightarrow{p} \mathbb{E}[Y_{it} | D_i = d, T_t]
$$

因此 $\hat{\tau}_{\text{DID}} \xrightarrow{p} \tau_{\text{ATT}}$，即 DID 估计量是 ATT 的一致估计。$\square$
> **直观理解（现实类比）**：DID（双重差分）就像"两条平行跑道赛跑"——一条是实验组、一条是对照组，政策只在实验组发令。先看两组的"起跑线差距"（政策前的固有差），再看政策后两组的"成绩变化"，把后者减去前者，剩下的就是政策真正多拉出的那段距离 $	au_{	ext{ATT}}$。关键前提是"平行趋势"：要是没政策，两条跑道本该齐头并进。

---

## 4. 示例

### 示例 1：用 OLS 估计教育回报率

**问题**：使用 Mincer 工资方程估计教育回报率。数据来自某调查，包含 $n = 1000$ 个个体，变量包括对数工资（$\ln(\text{wage})$）、受教育年限（educ）、工作经验（exper）及其平方项。

**解**：

**步骤1：Mincer 方程设定**

$$
\ln(\text{wage}_i) = \beta_0 + \beta_1 \text{educ}_i + \beta_2 \text{exper}_i + \beta_3 \text{exper}_i^2 + \varepsilon_i
$$

其中 $\beta_1$ 即为教育回报率——每增加一年教育，工资增长的百分比。

**步骤2：数据描述**

| 变量 | 均值 | 标准差 | 最小值 | 最大值 |
|:---:|:---:|:---:|:---:|:---:|
| ln(wage) | 2.45 | 0.52 | 0.85 | 3.95 |
| educ | 13.2 | 2.8 | 6 | 20 |
| exper | 18.5 | 10.3 | 1 | 45 |

**步骤3：OLS 估计**

使用 OLS 估计得到：

| 变量 | 系数 | 标准误 | t 统计量 | p 值 |
|:---:|:---:|:---:|:---:|:---:|
| const | 0.824 | 0.112 | 7.36 | < 0.001 |
| educ | 0.087 | 0.008 | 10.88 | < 0.001 |
| exper | 0.041 | 0.005 | 8.20 | < 0.001 |
| exper² | -0.0007 | 0.0001 | -7.00 | < 0.001 |

**步骤4：结果解释**

- **教育回报率**：$\hat{\beta}_1 = 0.087$，即每多受一年教育，工资平均增长 8.7%。这符合经典文献中 8-10% 的估计范围。
- **工作经验**：经验的一次项为正（0.041），二次项为负（-0.0007），表明工资随经验增长先增后减，拐点约在 $41/(2 \times 0.7) \approx 29.3$ 年处。
- 模型整体 $R^2 = 0.342$，即教育和工作经验解释了约 34.2% 的工资变异。

**步骤5：内生性讨论**

OLS 估计可能因遗漏能力变量（ability bias）而存在内生性问题。能力更高的人通常获得更多教育且工资更高，导致 $\text{Cov}(\text{educ}, \varepsilon) > 0$，使 OLS 估计高估教育回报率。工具变量方法（如使用同卵双胞胎数据或义务教育法作为工具变量）可解决这一问题。

**步骤6：稳健标准误**

使用异方差稳健标准误（Huber-White 标准误）重新计算，教育回报率的标准误为 0.009，结论不变。

---

### 示例 2：用 DID 评估政策效果

**问题**：某州在 2020 年实施了一项最低工资上调政策。使用 DID 方法评估该政策对就业的影响。数据包含处理州（实施最低工资上调）和对照州（未实施）在 2019 年（政策前）和 2021 年（政策后）的就业率数据。

**解**：

**步骤1：数据描述**

| 组别 | 时期 | 就业率（均值） | 样本量 |
|:---:|:---:|:---:|:---:|
| 处理组 | 2019（政策前） | 95.2% | 500 |
| 处理组 | 2021（政策后） | 94.1% | 500 |
| 对照组 | 2019（政策前） | 96.8% | 800 |
| 对照组 | 2021（政策后） | 96.5% | 800 |

**步骤2：DID 计算**

处理组变化：$\Delta_{\text{处理}} = 94.1\% - 95.2\% = -1.1\%$

对照组变化：$\Delta_{\text{对照}} = 96.5\% - 96.8\% = -0.3\%$

DID 估计量：$\hat{\tau}_{\text{DID}} = (-1.1\%) - (-0.3\%) = -0.8\%$

**步骤3：回归估计**

使用以下回归方程：

$$
Y_{it} = \alpha + \beta \cdot \text{Post}_t + \gamma \cdot \text{Treat}_i + \tau \cdot (\text{Treat}_i \times \text{Post}_t) + \varepsilon_{it}
$$

OLS 估计结果：

| 变量 | 系数 | 标准误 | t 统计量 |
|:---:|:---:|:---:|:---:|
| const | 96.8 | 0.15 | 645.3 |
| Post | -0.3 | 0.21 | -1.43 |
| Treat | -1.6 | 0.24 | -6.67 |
| Treat × Post | -0.8 | 0.34 | -2.35 |

交互项系数 $\tau = -0.8$ 即为 DID 估计量，在 5% 水平上显著（p = 0.019），表明最低工资上调导致就业率下降了约 0.8 个百分点。

**步骤4：平行趋势检验**

为验证平行趋势假设，绘制处理组和对照组在政策实施前（2015-2019 年）的就业率变化趋势：

| 年份 | 处理组 | 对照组 | 差异 |
|:---:|:---:|:---:|:---:|
| 2015 | 96.0% | 97.5% | -1.5% |
| 2016 | 95.8% | 97.3% | -1.5% |
| 2017 | 95.5% | 97.0% | -1.5% |
| 2018 | 95.3% | 96.9% | -1.6% |
| 2019 | 95.2% | 96.8% | -1.6% |

两组差异在政策前保持稳定（约 1.5-1.6 个百分点），平行趋势假设成立。

**步骤5：安慰剂检验**

进行安慰剂检验（placebo test），假设政策发生在 2018 年（实际上未发生），重新计算 DID：

$$
\hat{\tau}_{\text{安慰剂}} = (95.3\% - 95.5\%) - (96.9\% - 97.0\%) = (-0.2\%) - (-0.1\%) = -0.1\%
$$

系数不显著（p = 0.76），支持政策的因果效应。

---

## 5. 习题

### 基础题

**习题 1**（线性回归与 OLS）写出线性回归模型 $y_i=x_i^\top\beta+\varepsilon_i$ 与 OLS 估计量 $\hat\beta_{\mathrm{OLS}}$ 的公式，说明严格外生性、球形误差与满秩三个基本假设各自的含义。

**习题 2**（拟合优度）解释 $R^2$ 的含义，说明 $R^2=0.342$ 表示什么，并指出用 $R^2$ 比较两个含不同解释变量个数模型时的问题。

**习题 3**（内生性）给出内生性的定义，说明当解释变量与误差项相关时 OLS 为何不一致，并举例。

**习题 4**（工具变量条件）写出工具变量 $z$ 须满足的"相关性"与"外生性"两个条件，分别说明违反哪个条件会导致什么后果。

**习题 5**（2SLS 步骤）描述两阶段最小二乘的两个阶段，说明第二阶段为何用拟合值 $\hat x$ 而非原 $x$。

**习题 6**（面板效应）从模型 $y_{it}=x_{it}^\top\beta+\alpha_i+\varepsilon_{it}$ 出发，说明固定效应与随机效应对 $\alpha_i$ 与 $x_{it}$ 相关性的不同假设，以及违反随机效应假设的后果。

**习题 7**（DID 估计量）写出 DID 估计量 $\hat\tau_{\mathrm{DID}}$ 的公式，并陈述识别它所需的关键假设。

**习题 8**（断点回归）说明 RDD 利用分配变量在阈值 $c$ 的跳跃识别局部处理效应的思想，写出 $\tau_{\mathrm{RDD}}$ 的定义式。

**习题 9**（协整）说明 $I(1)$ 过程与协整的定义，解释"若 $y_t-\beta x_t\sim I(0)$ 则二者协整"的含义及其对伪回归的补救作用。

**习题 10**（离散与计数模型）区分 Logit 与 Probit 模型，说明它们分别对应哪个分布函数；再说明泊松回归适用的数据类型。

### 进阶题

**习题 11**（OLS 方差）对一元回归 $y_i=\beta_0+\beta_1x_i+\varepsilon_i$，$ \varepsilon_i$ i.i.d. 均值为 0 方差 $\sigma^2$，推导并说明
$$\mathrm{Var}(\hat\beta_1)=\frac{\sigma^2}{\sum_{i=1}^n(x_i-\bar x)^2}$$

**习题 12**（假设检验）说明对"教育回报率 $\beta_1$ 是否为零"作 t 检验的步骤：原假设、检验统计量 $t=\frac{\hat\beta_1}{\mathrm{se}(\hat\beta_1)}$、p 值的含义，以及当 $|t|$ 很大时为何拒绝原假设。已知 $\hat\beta_1=0.087,\mathrm{se}=0.008$，计算 t 并判断显著性。

**习题 13**（DID 数值计算）处理州政策前就业率 $95.2\%$、后 $94.1\%$，对照州前 $96.8\%$、后 $96.5\%$。计算 DID 估计量并解释其经济含义。

**习题 14**（Hausman 检验思想）解释 Hausman 检验如何利用"$\hat\beta_{\mathrm{FE}}$ 在 $\alpha_i$ 与 $x$ 相关时也一致，而 $\hat\beta_{\mathrm{RE}}$ 仅在 $\alpha_i$ 与 $x$ 不相关时一致"来在固定效应与随机效应之间选择，写出检验的核心对比量。

**习题 15**（Logit 预测概率）给定 Logit 模型 $P(y=1\mid x)=\frac{e^{x^\top\beta}}{1+e^{x^\top\beta}}$，设某个体 $x^\top\beta=1.1$，计算其预测概率 $P$。

**习题 16**（IV 两阶段计算）两阶段中第一阶段回归 $x_i=\gamma z_i+\eta_i$，由样本得到 $\hat\gamma$ 与拟合 $\hat x_i$。说明第二阶段 $y_i=\hat\beta_0+\hat\beta_1\hat x_i+\nu_i$ 中 $\hat\beta_1$ 为何能校正发散的控制，简要写出 $\hat\beta_{\mathrm{IV}}$ 的矩阵形式。

**习题 17**（异方差）说明异方差为何使 OLS 的常规标准误无效，写出异方差稳健（Huber-White）标准误的思想依据，指数"教育、经验"例子中为何用稳健标准误。

**习题 18**（多重共线性）以个人收入回归中的教育、经验、证书为例，说明多重共线性如何影响 $\hat\beta$ 的方差与符号稳定性，说明 VIF 的诊断逻辑。

**习题 19**（误差修正模型）写出 ECM：$\Delta y_t=\alpha(y_{t-1}-\beta x_{t-1})+\gamma\Delta x_t+\varepsilon_t$，解释 $\alpha<0$ 表示的意义（误差修正作用）。

### 挑战题

**习题 20**（OLS 方差证明）证明一元回归中 OLS 斜率估计量的方差公式，需将 $\hat\beta_1$ 表为 $\beta_1+\frac{\sum_i(x_i-\bar x)\varepsilon_i}{\sum_i(x_i-\bar x)^2}$ 再取方差。

**习题 21**（IV 一致性证明）在恰好识别 $L=K$ 情形下，写出 IV 估计量 $\hat\beta_{\mathrm{IV}}=(Z^\top X)^{-1}Z^\top y$，利用矩条件 $\mathbb E[z_i\varepsilon_i]=0$ 与弱大数定律证明 $\hat\beta_{\mathrm{IV}}\xrightarrow{p}\beta_0$。

**习题 22**（GMM- IV 等价）说明在恰好识别 $L=K$ 时，GMM 估计量为何与 IV 一致且不依赖权矩阵 $W$：由一阶条件 $\hat G^\top W\bar g(\hat\theta)=0$ 与 $\hat G$ 可逆推得 $\bar g(\hat\theta)=0$。

**习题 23**（FE-FD 等价）设 $T=2$，证明固定效应（组内）估计量等价于一阶差分估计量 $\hat\beta_{\mathrm{FD}}=\left(\sum_i\Delta x_i\Delta x_i^\top\right)^{-1}\sum_i\Delta x_i\Delta y_i$，其中 $\Delta x_i=x_{i2}-x_{i1}$。

**习题 24**（DID-回归等价）证明在二值处理与两期下，回归
$$Y_{it}=\alpha+\beta\,\mathrm{Post}_t+\gamma\,\mathrm{Treat}_i+\tau\,(\mathrm{Treat}_i\times\mathrm{Post}_t)+\varepsilon_{it}$$
中交互项系数的 OLS 估计 $\hat\tau$ 恰等于 DID 估计量。

**习题 25**（AR(1) 一致性）对平稳 AR(1) $y_t=\rho y_{t-1}+\varepsilon_t$（$|\rho|<1$，$\varepsilon_t$ i.i.d. 均值为 0），证明 OLS 估计量 $\hat\rho$ 一致，并推导其渐近分布（给出中心极限论证思路）。

**习题 26**（GMM 最优权）设 GMM 渐近方差 $V(W)=(G^\top WG)^{-1}G^\top WSWG(G^\top WG)^{-1}$，用 Cauchy-Schwarz 论证当 $W=S^{-1}$ 时 $V$ 最小，即最优 GMM 渐近方差 $(G^\top S^{-1}G)^{-1}$。

### 探究题

**习题 27**（因果推断）讨论 Rubin 潜在结果框架 $Y_i(1),Y_i(0)$ 与"个体处理效应不可观"的困境，说明观测性研究中"选择偏差"的来源及随机对照试验为何是金标准。

**习题 28**（RDD 假设）讨论 RDD 的关键识别假设（连续性、无操纵 cut-off）以及"局部"处理效应的含义，说明其效度边界与检验（如 McCrary 密度检验）的重要性。

**习题 29**（机器学习计量）探讨双机器学习（Double/Debiased ML）如何在含高维混杂时估计处理效应，说明 Neyman 正交得分与交叉拟合如何降低过拟合偏误，把估计法与传统 OLS 作对比。

**习题 30**（复现危机与研究设计）讨论计量经济学中 p 值滥用、多重比较与复现失败现象，说明预先注册、公开数据代码、稳健与敏感性分析如何改善推断可信度。提出一个可验证的研究问题以检验某因果主张的稳健性。

---

## 6. 习题答案与解析

### 基础题答案

1. **线性回归与 OLS** $\hat\beta_{\mathrm{OLS}}=(X^\top X)^{-1}X^\top y$。严格外生性 $\mathbb E[\varepsilon\mid X]=0$ 保证一致性；球形误差 $\mathrm{Var}(\varepsilon\mid X)=\sigma^2 I_n$（同方差且不相关）保证 OLS 有效并给出正确标准误；满秩 $\mathrm{rank}(X)=K$ 保证 $X^\top X$ 可逆、参数唯一可识别。

2. **拟合优度** $R^2$ 是被解释变量的总变异中由回归解释的比例，$R^2=0.342$ 说明约 34.2% 的工资变异被教育与经验解释。用 $R^2$ 比较不同解释变量个数不理想，因为没有惩罚加入的变量、单调不减，宜用调整 $R^2$ 或信息准则比较。

3. **内生性** 内生性是解释变量与误差项相关：$\mathrm{Cov}(x_i,\varepsilon_i)\neq0$。此时 $\frac1n\sum x_i\varepsilon_i$ 不收敛于 0，OLS 的 $\hat\beta-\beta=\left(\frac{X^\top X}{n}\right)^{-1}\frac{X^\top\varepsilon}{n}$ 中分子不趋于 0，故 $\hat\beta\not\xrightarrow{p}\beta$（不一致）。例：能力潜变量同时影响教育年限与工资，遗漏能力使 $\mathrm{Cov}(\text{educ},\varepsilon)>0$，高估教育回报。

4. **工具变量条件** 相关性：$\mathrm{Cov}(z,x)\neq0$（$z$ 确实影响 $x$），违反则第一阶段无变异性、$\hat\beta_{\mathrm{IV}}$ 不存在或极不稳定；外生性：$\mathbb E[\varepsilon\mid z]=0$（$z$ 只通过 $x$ 影响 $y$），违反则矩条件 $\mathbb E[z\varepsilon]=0$ 失效、IV 同样不一致。两条缺一不可。

5. **2SLS 步骤** 第一阶段：用工具 $z$ 回归 $x$，得到拟合值 $\hat x=Z(Z^\top Z)^{-1}Z^\top x$；第二阶段：用 $\hat x$ 回归 $y$ 得 $\hat\beta_{2SLS}$。用拟合值 $\hat x$ 是因为 $\hat x$ 只含由 $z$ 解释"干净"的部分，与 $\varepsilon$ 不相关（$z$ 外生），从而消除原 $x$ 中与误差相关的内生成分。

6. **面板效应** 随机效应假设 $\alpha_i$ 与 $x_{it}$ 不相关，可用 RE 得有效估计；固定效应允许 $\alpha_i$ 与 $x_{it}$ 任意相关（个体固定特质），用组内变换消去 $\alpha_i$。若真实相关却用 RE，则 $\alpha_i$ 进入误差项并与 $x$ 相关，导致 OLS/RE 不一致，故相关时须用 FE。

7. **DID 估计量** $$\hat\tau_{\mathrm{DID}}=(\bar y_{\text{处理,后}}-\bar y_{\text{处理,前}})-(\bar y_{\text{对照,后}}-\bar y_{\text{对照,前}})$$ 关键假设为平行趋势：没有处理时，处理组与对照组结果的变化趋势相同（反事实趋势），这样对比组差分才能分离纯政策效应。

8. **断点回归** RDD 在被处理或被控制的分配变量 $x$ 恰过阈值 $c$ 时比较前后结果，$$\tau_{\mathrm{RDD}}=\lim_{x\downarrow c}\mathbb E[y\mid x]-\lim_{x\uparrow c}\mathbb E[y\mid x]$$ 在阈值两侧的个体除是否接受处理外"几乎一样"，跳跃即处理效应。

9. **协整** $I(1)$ 指该序列非平稳且一次差分后平稳。若 $y_t,x_t$ 均 $I(1)$ 但存在 $\beta$ 使 $y_t-\beta x_t\sim I(0)$（平稳），则二者协整：长期存在稳定的均衡关系，短期偏离会被纠正。用协整与 ECM 回归可避免对非平稳数据直接 OLS 出现的伪回归。

10. **离散与计数模型** Logit 用逻辑斯蒂分布 $\frac{e^{x^\top\beta}}{1+e^{x^\top\beta}}$，Probit 用标准正态累积 $\Phi(x^\top\beta)$，二者都对二元因变量建模，尾部略有差异。泊松回归用于非负整数计数数据，$\lambda_i=e^{x_i^\top\beta}$，刻画发生率（如事故数、就诊次数）。

### 进阶题答案

1. **OLS 方差** 由 $\hat\beta_1=\beta_1+\frac{\sum_i(x_i-\bar x)\varepsilon_i}{\sum_i(x_i-\bar x)^2}$（分子权重 $\sum_i(x_i-\bar x)=0$ 消去截距）。因 $\varepsilon_i$ 相互独立且与 $x$ 独立，
$$\mathrm{Var}(\hat\beta_1)=\frac{\sum_i(x_i-\bar x)^2\sigma^2}{\left(\sum_i(x_i-\bar x)^2\right)^2}=\frac{\sigma^2}{\sum_i(x_i-\bar x)^2}$$
可见解释变量变异越大、误差方差越小，估计越准。

2. **假设检验** 步骤：$H_0:\beta_1=0$，$H_1:\beta_1\neq0$；构造 $t=\frac{\hat\beta_1}{\mathrm{se}(\hat\beta_1)}$，在大样本下近似 $N(0,1)$（或用 $t$ 分布）；p 值是在 $H_0$ 下观测到如此极端统计量的概率。给定 $\hat\beta_1=0.087,\mathrm{se}=0.008$，$t=0.087/0.008=10.875$，远超常见临界值（$1.96$），p 值极小，拒绝 $H_0$，教育回报显著非零。

3. **DID 数值计算** 处理组变化 $-1.1\%$，对照组变化 $-0.3\%$，$$\hat\tau_{\mathrm{DID}}=(-1.1\%)-(-0.3\%)=-0.8\%$$ 即在平行趋势假设下，政策使处理组就业率相对下降约 0.8 个百分点（负向效应），且两组的共同趋势波动已被差分消去。

4. **Hausman 检验思想** 用组内拟合的 $\hat\beta_{\mathrm{FE}}$ 在 $\alpha_i$ 与 $x$ 相关时一致，而 $\hat\beta_{\mathrm{RE}}$ 仅在 $\alpha_i\perp x$ 时一致；原假设（RE 成立，即正交）下两者都一致、应相近：$H=\left(\frac{\hat\beta_{\mathrm{FE}}-\hat\beta_{\mathrm{RE}}}{\mathrm{se}(\hat\beta_{\mathrm{FE}}-\hat\beta_{\mathrm{RE}})}\right)^2$，大样本渐近 $\chi^2$。$H$ 大则拒绝正交假设、选 FE；$H$ 小则 RE 更有效、选 RE。

5. **Logit 预测概率** $$P(y=1)=\frac{e^{1.1}}{1+e^{1.1}}=\frac{3.0042}{1+3.0042}\approx0.7502$$ 即该个体被预测为 $y=1$ 的概率约 75%，高于 50% 属于"事件"一侧。

6. **IV 两阶段计算** 第一阶段的 $\hat\gamma,\hat x_i$ 提取 $x$ 中与 $z$ 相关的干净变异；第二阶段用 $\hat x$ 回归，因 $\mathrm{Cov}(\hat x,\varepsilon)\approx0$ 消除内生偏误。矩阵形式 $$\hat\beta_{\mathrm{IV}}=(X^\top Z(Z^\top Z)^{-1}Z^\top X)^{-1}X^\top Z(Z^\top Z)^{-1}Z^\top y$$ 当只有一个工具时退化为两阶段路径，给出校正后的一致的因果参数估计。

7. **异方差** 异方差使常规标准误 $\mathrm{se}|_{OLS}$ 失效（$t$、$F$ 检验失真），因为它假设 $\mathrm{Var}(\varepsilon_i)=\sigma^2$ 恒定。Huber-White 用残差平方替代总体方差、构造稳健方差 $\left(\sum \hat x_i\hat x_i^\top\right)^{-1}\left(\sum \hat x_i\hat x_i^\top\hat\varepsilon_i^2\right)\left(\sum\hat x_i\hat x_i^\top\right)^{-1}$。教育报酬回归中因工资异质性大，用稳健标准误使推断更可信。

8. **多重共线性** 共线性使 $X^\top X$ 接近奇异、$(X^\top X)^{-1}$ 变得巨大，故 $\hat\beta$ 方差膨胀、符号与幅度不稳定、系数标准误很大但整体 $R^2$ 可很高。VIF 反映第 $j$ 个回归量被其他回归量解释的程度，$\mathrm{VIF}_j=\frac{1}{1-R_j^2}$，$VIF>10$ 通常视为强共线，提示需合并或剔除冗余变量。

9. **误差修正模型** $\alpha$ 为误差修正系数，$\alpha<0$ 表示当 $t-1$ 期 $y$ 高于长期均衡 $(\beta x)$ 时（$y_{t-1}-\beta x_{t-1}>0$），$t$ 期会有 $\alpha$ 倍的负向调整，把系统拉回均衡；$\alpha$ 绝对值越大修正越快。$y$ 与 $x$ 协整时 $\alpha\neq0$ 确保偏离不会无限累积的是举足轻重的平衡机制。

### 挑战题答案

1. **OLS 方差证明** 由 $\hat\beta_1=\frac{\sum_i(x_i-\bar x)y_i}{\sum_i(x_i-\bar x)^2}$ 代入 $y_i=\beta_0+\beta_1x_i+\varepsilon_i$，利用 $\sum(x_i-\bar x)=0$ 与 $\sum(x_i-\bar x)(\beta_0+\beta_1x_i)=\beta_1\sum(x_i-\bar x)^2$ 得 $\hat\beta_1=\beta_1+\frac{\sum_i(x_i-\bar x)\varepsilon_i}{\sum_i(x_i-\bar x)^2}$。因 $\varepsilon_i$ i.i.d. 均值为 0 且与 $x$ 独立、方差 $\sigma^2$，交叉项期望为 0，故 $$\mathrm{Var}(\hat\beta_1)=\frac{\sum_i(x_i-\bar x)^2\sigma^2}{\left(\sum_i(x_i-\bar x)^2\right)^2}=\frac{\sigma^2}{\sum_i(x_i-\bar x)^2}$$ 证毕，$x$ 变异越大约定越精确。

2. **IV 一致性证明** 代入 $y=X\beta_0+\varepsilon$：$$\hat\beta_{\mathrm{IV}}-\beta_0=\left(\frac{Z^\top X}{n}\right)^{-1}\frac{Z^\top\varepsilon}{n}$$ 由相关性假设 $\frac{Z^\top X}{n}\xrightarrow{p}\Sigma_{zx}$（满秩可逆）；由外生性 $\mathbb E[z_i\varepsilon_i]=0$ 与弱大数定律 $\frac{Z^\top\varepsilon}{n}\xrightarrow{p}0$。故 $\hat\beta_{\mathrm{IV}}-\beta_0\xrightarrow{p}\Sigma_{zx}^{-1}\cdot0=0$，即 $\hat\beta_{\mathrm{IV}}\xrightarrow{p}\beta_0$，一致。

3. **GMM-IV 等价** 恰好识别时 $\hat G\in\mathbb R^{K\times K}$ 为方阵且满秩可逆。GMM 一阶条件 $\hat G^\top W\bar g(\hat\theta)=0$，两边左乘 $(\hat G^\top)^{-1}$ 得 $W\bar g(\hat\theta)=0$，$W$ 正定故 $\bar g(\hat\theta)=0$，即 $\hat\theta$ 恰解出矩条件，同 IV。因 $W$ 化简掉了、方程不含 $W$，故估计结果与权矩阵选择无关，这正是一致性的保证。

4. **FE-FD 等价** $T=2$ 时组内变换 $\ddot x_{i1}=x_{i1}-\bar x_i=-\frac12\Delta x_i$，$\ddot x_{i2}=+\frac12\Delta x_i$（$\Delta x_i=x_{i2}-x_{i1}$）。组内估计 $\hat\beta_{\mathrm{FE}}=\left(\sum_i\sum_t\ddot x_{it}\ddot x_{it}^\top\right)^{-1}\sum_i\sum_t\ddot x_{it}\ddot y_{it}$ 中分子分母的 $\frac12$ 因子上下抵消，恰等于一阶差分形式 $\left(\sum_i\Delta x_i\Delta x_i^\top\right)^{-1}\sum_i\Delta x_i\Delta y_i$，故 $T=2$ 时二者完全相同。

5. **DID-回归等价** 记四组均值 $\bar Y_{11},\bar Y_{10},\bar Y_{01},\bar Y_{00}$。回归中 $\hat\tau$ 是处理组在政策后的额外跳跃，线性模型的最小二乘可分解为四组均值的组合，展开可得 $\hat\tau=(\bar Y_{11}-\bar Y_{10})-(\bar Y_{01}-\bar Y_{00})$，即恰等于 DID 估计量。交互项 $\mathrm{Treat}\times\mathrm{Post}$ 的系数捕捉政策对处理组的增量影响，回归框架的优点是可直接给出标准误与平行趋势扩展。

6. **AR(1) 一致性** $\hat\rho=\rho+\frac{\sum y_{t-1}\varepsilon_t}{\sum y_{t-1}^2}$。平稳性下 $\frac1n\sum y_{t-1}\varepsilon_t\xrightarrow{p}0$（$\varepsilon_t$ 与 $y_{t-1}$ 正交）而 $\frac1n\sum y_{t-1}^2\xrightarrow{p}\frac{\sigma^2}{1-\rho^2}>0$，故一致性。渐近分布：$\sqrt n(\hat\rho-\rho)=\frac{\frac1{\sqrt n}\sum y_{t-1}\varepsilon_t}{\frac1n\sum y_{t-1}^2}$，分子是鞅差序列部分和，由鞅差 CLT 收敛到 $N(0,\mathbb E[y_{t-1}^2\varepsilon_t^2])=N(0,\frac{\sigma^4}{1-\rho^2})$，分母收敛到 $\frac{\sigma^2}{1-\rho^2}$，Slutsky 得 $\sqrt n(\hat\rho-\rho)\xrightarrow{d}N(0,1-\rho^2)$。

7. **GMM 最优权** 设 $A=S^{1/2}G(G^\top S^{-1}G)^{-1}$，$B=S^{1/2}WG(G^\top WG)^{-1}$，则 $V(W)=B^\top B$。由 Cauchy-Schwarz $B^\top B\ge(A^\top B)(A^\top A)^{-1}(A^\top B)^\top$，计算 $A^\top B=I$、$A^\top A=(G^\top S^{-1}G)^{-1}$，故 $$V(W)\ge(G^\top S^{-1}G)^{-1}$$ 等号当 $W\propto S^{-1}$ 时取到。故用 $W=S^{-1}$（或一致估计）所得最优 GMM 渐近方差最小，等于 $(G^\top S^{-1}G)^{-1}$，即理论最优效率。

### 探究题答案

1. **因果推断** 潜在结果框架定义 $Y_i(1),Y_i(0)$ 为个体 $i$ 在两种对照状态下的结果，个体处理效应 $\tau_i=Y_i(1)-Y_i(0)$ 因只能观测其一而不可直接观测，只能估计平均效应 ATT/ATE。观测数据中，处理与否常由自选择决定（如受教育者本身更优、更自律），处理组与对照组的反事实不可比，形成选择偏差。随机对照试验通过随机化使处理分配独立于潜在结果，从而两组"可交换"，差分均值给出无偏因果效应，故为金标准。

2. **RDD 假设** RDD 的关键假设是 (i) 潜在结果的条件均值在阈值连续（无其他同时跳跃的处理/设备），(ii) 个体无法精确操纵分配变量（无 manipulation），保证阈值两侧个体可视为随机可比；(iii) 只识别阈值附近的局部平均处理效应 LATE，外推到更远范围需附加假设。检验（如 McCrary 密度检验分配变量在阈值两侧分布连续性）与带宽/核方法选择决定其效度，边界使用者须警惕操纵与"干扰平稳性"。

3. **机器学习计量** Double/Debiased ML 用机器学习分别估计"结果对混杂的回归"与"处理对混杂的回归"，构造 Neyman 正交得分（残差化的 $\tau$ 估计），使一阶偏误退化；交叉拟合避免在预测与估计上重复用同一子样本造成的过拟合偏误。相比把高维可控变量直接放进 OLS（会导致过拟合、$\tau$ 有偏且方差被低估），DML 在 $n$ 与协变量维度大时仍给出近似无偏且渐近正态的处理效应估计，配合半参数理论给出有效推断。

4. **复现危机与研究设计** p 值滥用（p-hacking、选择性报告、多重比较）与复现失败损害计量推断的可信度。缓解手段包括：预先注册分析计划以约束事后选择、公布数据与代码便于复现、做基准情形的稳健/敏感性分析、报告效应量与置信区间而非只看显著性哑变量、用多重检验校正控制假阳性。可验证问题示例：对"义务教育法作为工具变量估计教育回报"的经典结论，用其公开数据重做：添加敏感性分析（改变划分规则、放宽排除性约束）与安慰剂、并用多重推断校正，检验原 $\hat\beta_1$ 的稳健性是否仍成立。

---

## 7. 总结

本教程系统介绍了计量经济学的核心理论与方法：

1. **线性回归模型与 OLS 估计**：
   - OLS 估计量具有显式解 $\hat{\beta} = (X^\top X)^{-1} X^\top y$
   - 在鞅差序列假设下，OLS 估计量一致且渐近正态
   - 异方差和序列相关需要通过稳健标准误进行校正

2. **工具变量与 2SLS**：
   - 解决内生性问题的标准方法
   - 工具变量需满足相关性和外生性两个条件
   - 矩条件框架为 IV 估计提供了统一的数学基础

3. **广义矩方法（GMM）**：
   - 将矩条件推广到一般情形
   - 最优权矩阵 $W = S^{-1}$ 使估计量渐近有效
   - 过度识别可通过 J 统计量进行检验

4. **面板数据模型**：
   - 固定效应通过组内变换消去个体异质性
   - 随机效应假设个体效应与解释变量不相关
   - Hausman 检验用于选择 FE 或 RE

5. **因果推断方法**：
   - DID 利用平行趋势假设识别处理效应
   - 平行趋势检验和安慰剂检验是 DID 的标准诊断工具
   - RDD 利用阈值的跳跃识别局部处理效应

6. **时间序列计量经济学**：
   - 协整和误差修正模型处理非平稳时间序列
   - VAR 模型分析多变量时间序列的动态关系

7. **离散选择与计数数据**：
   - Logit 和 Probit 模型处理二元选择
   - 泊松回归处理计数数据

计量经济学是经济学实证研究的方法论基础，其核心在于从观测数据中推断因果关系。掌握这些工具将为深入进行实证研究奠定坚实基础。